"""Example module — replace with your own code.

Exists so that docstring-coverage checks (interrogate) have something
to count in a freshly generated project. Delete or replace once you
start adding real modules.
"""


def greet(name: str) -> str:
    """Return a friendly greeting for *name*.

    Args:
        name: Name of the person to greet.

    Returns:
        A greeting string like ``"Hello, Alice!"``.
    """
    return f"Hello, {name}!"
