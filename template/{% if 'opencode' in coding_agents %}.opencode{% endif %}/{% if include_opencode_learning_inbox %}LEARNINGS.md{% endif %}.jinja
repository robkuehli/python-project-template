# Lessons Learned
# Ort: .opencode/LEARNINGS.md  ·  in den Kontext geladen via `instructions:` in opencode.json
#
# Format pro Eintrag (neueste zuerst, kurz & handlungsrelevant):
#   <!-- YYYY-MM-DD | projekt | was schieflief -->
#   - Konkrete Regel, die den Fehler künftig verhindert
#
# Geschrieben wird hier NUR über /capture (manueller Promote). Vorschläge sammeln
# sich vorher in LEARNINGS.inbox.md. OpenCode hat keine native Auto-Memory —
# diese Datei ersetzt die private Memory-Ebene für OpenCode (git-tracked,
# projekt-lokal). Team-Regeln gehören weiterhin nach guidelines/*.md / AGENTS.md.

## Regeln aus echten Fehlern

<!-- Einträge hier einfügen -->